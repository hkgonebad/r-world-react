import HeaderNavbar from "./HeaderNavbar";
import HeaderTicker from "./HeaderTicker";

const Header = () => {
  return (
    <header>
      {/* Header Navbar */}
      <HeaderNavbar />

      {/* Header Ticker */}
      <HeaderTicker />
    </header>
  );
};

export default Header;
